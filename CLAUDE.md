# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## REGLA OBLIGATORIA: Persistencia de contexto

**ANTES de considerar la tarea terminada, SIEMPRE ejecutar estos dos pasos. No son opcionales.**

1. **Actualizar CLAUDE.md** si hubo: nueva funcionalidad, decision arquitectonica, nuevo patron, cambio de workflow, nuevo modulo, o cualquier informacion que una sesion futura necesitaria para no romper cosas.
2. **Actualizar memory/** si hubo: feedback del usuario sobre como trabajar, preferencias nuevas, contexto de proyecto (deadlines, estado actual), o referencias externas utiles.

**Si no hay nada que actualizar, decir explicitamente: "No hay cambios para CLAUDE.md ni memory."** Esto confirma que se evaluo y se decidio no actualizar, en vez de olvidarlo.

## REGLA OBLIGATORIA: Validacion de requerimientos

**Cuando el usuario entregue un paquete de instrucciones o requisitos, ANTES de planear o implementar:**

1. Revisar los requisitos contra el codigo actual y CLAUDE.md.
2. Identificar: requisitos faltantes, contradicciones con patrones existentes, edge cases no cubiertos, side-effects con otros modulos.
3. Si hay gaps, listarlos y esperar confirmacion antes de continuar.
4. Si no hay gaps, decir "Requisitos validados, no encontre gaps" y proceder.

---

## REGLA OBLIGATORIA: Tests en toda funcionalidad nueva

**Toda implementacion de funcionalidad nueva o modificacion significativa DEBE incluir tests que cubran:**

1. **Caso feliz**: flujo completo con datos validos (crear, liquidar, cancelar, etc.).
2. **Validaciones**: campos obligatorios, tipos invalidos, valores fuera de rango → esperar 422.
3. **Edge cases**: limites del negocio (stock negativo, balance cero, duplicados, estados invalidos).
4. **Side-effects cross-module**: verificar que operaciones que afectan otros modulos (balances, inventario, costos) producen los efectos esperados.
5. **Permisos RBAC**: si se agregan o modifican permisos, testear acceso permitido Y denegado.

**Si el paquete de instrucciones incluye criterios de aceptacion (seccion 7), cada criterio debe tener al menos un test correspondiente.**

Excepciones: cambios puramente cosmeticos en frontend (CSS, labels, reordenar UI) no requieren tests backend.

---

## REGLA OBLIGATORIA: Responsive mobile-first en toda feature nueva

**Toda pagina, formulario, tabla, modal o componente nuevo DEBE ser usable en 390px (iPhone 12+) ademas de desktop.** Mobile no es opcional ni "fase futura" — se construye al mismo tiempo que la version desktop. Verificar en DevTools mobile mode antes de marcar la tarea como completada.

**Patrones ya establecidos (reutilizar, no reinventar):**

- **Hook `useIsMobile()`** ([frontend/src/hooks/useMediaQuery.ts](frontend/src/hooks/useMediaQuery.ts)) — `boolean`, true bajo `md=768px`. Solo para logica imperativa; 95% de los casos se resuelven con clases Tailwind responsive.
- **Tablas con >4 columnas en mobile**:
  - Opcion A (preferida para listados de operaciones/movimientos): dual render `<div className="hidden md:block">` (Table desktop) + `<div className="md:hidden space-y-2">` (cards mobile), o `<DataTable renderMobileCard={(row) => ...} />`.
  - Opcion B (reportes anchos, tablas con sticky col): overflow wrapper `<div className="overflow-x-auto -mx-3 sm:mx-0"><Table className="min-w-[Npx]">`.
  - Cards mobile ya implementadas: `<OperationListCard>` (purchases/sales/double-entries) y `<MovementListCard>` (treasury). Reutilizar antes de crear nuevas.
- **Formularios multi-linea**: `<FormLineGrid>` ([frontend/src/components/shared/FormLineGrid.tsx](frontend/src/components/shared/FormLineGrid.tsx)) — `grid-cols-1` mobile, `md:grid-cols-12` desktop. Hijos anotados con `md:col-span-N`.
- **Filtros**: `flex flex-col sm:flex-row sm:flex-wrap gap-2 sm:items-center`. DateRangePicker / Select / Input: `w-full sm:w-auto` (o `sm:w-40`).
- **Grids de KPIs / info cards**: `grid-cols-1 sm:grid-cols-2 md:grid-cols-4`. **NUNCA** `grid-cols-2 md:grid-cols-4` — aprieta en 390px.
- **Dialogs shadcn**: el base ya incluye `w-[calc(100vw-1.5rem)] max-w-lg sm:w-full`. No agregar `max-w-md` que rompe el ajuste mobile. Internos: `grid-cols-1 sm:grid-cols-2` en vez de `grid-cols-2`.
- **Sticky bottom (Guardar/Cancelar en formularios)**: `sticky bottom-0 bg-white border-t -mx-3 px-3 md:-mx-6 md:px-6 pb-[max(1rem,env(safe-area-inset-bottom))]` + botones `w-full sm:w-auto`.
- **dt/dd en detail pages**: para valores largos (nombres, descripciones, materiales) usar `flex flex-col sm:flex-row sm:justify-between gap-0.5 sm:gap-3`. Para valores cortos (badges, montos, fechas, status) `flex justify-between gap-3` es correcto.
- **PageHeader**: `flex-col sm:flex-row` con botones de accion `flex-wrap`.
- **Inputs numericos**: `MoneyInput` ya tiene `inputMode={decimals > 0 ? "decimal" : "numeric"}` — abre teclado correcto en iOS. Reutilizar siempre que sea cantidad/precio/monto.
- **Tabs**: si son >4, envolver en `<div className="overflow-x-auto -mx-3 px-3 sm:mx-0 sm:px-0">` + `<TabsList className="inline-flex w-max sm:w-auto sm:flex-wrap">`.
- **Sticky cells en tablas anchas**: SIEMPRE con `bg-white` (o color de fila pares) — sticky transparente muestra el contenido detras al scrollear.
- **`id="main-scroll"` y `bg-slate-50/80` en `<main>` de Layout**: NO tocar — `useScrollRestoration` los necesita.

**Verificacion obligatoria antes de marcar tarea completada:**
1. DevTools mobile mode (iPhone 12, 390x844): sin overflow horizontal, hamburger funciona, formularios completables, tablas legibles (cards o scroll), botones primarios accesibles.
2. Desktop 1280px: comparar contra mismo componente antes del cambio — cero regresion visual.
3. Rotacion portrait↔landscape (si aplica): drawer/modals no quedan colgados.

---

## Guia para mantener CLAUDE.md

Al terminar una sesion donde se implemento funcionalidad nueva o se tomo una decision arquitectonica:

1. **Agregar decision nueva** al final de "Decisiones de Diseno" con el siguiente numero consecutivo. Incluir: que se hizo, por que, y como interactua con el resto del sistema. Ser conciso pero completo.
2. **Actualizar secciones existentes** si la nueva funcionalidad cambia algo ya documentado (ej: nuevo tipo de movimiento → actualizar la lista en Key Patterns, nuevo modulo → actualizar tabla de Business Modules, nuevos tests → actualizar conteo).
3. **No duplicar informacion**: si algo ya esta en Key Patterns, no repetirlo en Decisiones. Key Patterns = "que hace el sistema", Decisiones = "por que lo hace asi".
4. **Nunca desordenar la numeracion**: las decisiones se numeran secuencialmente. No saltar numeros, no duplicar.
5. **Condensar**: cada decision debe ser 2-4 lineas max. Si necesita mas, probablemente son 2 decisiones separadas.
6. **Conteos**: actualizar `Current: N tests` en Testing, y conteos de componentes/servicios/hooks en Frontend si cambian.
7. **Migrations**: solo mencionar migration ID si es relevante para entender la historia (ej: rename de tabla). No listar todas.
8. **Cache invalidation**: si la nueva funcionalidad crea side-effects cross-module, actualizar el mapa en `queryInvalidation.ts` Y documentarlo aqui.
9. **Migraciones**: si se crean nuevas migraciones, SIEMPRE correr `./venv/bin/alembic upgrade head` en la BD de desarrollo (5434) Y en la de test (5433) antes de probar. NUNCA correr migraciones directamente contra produccion — las migraciones en produccion se ejecutan SOLO via la skill `/deploy`. Los permisos RBAC se leen de la tabla `permissions` en BD, no del catalogo Python — sin migrar, los nuevos permisos no existen.

## Project Overview

EcoBalance is a multi-tenant ERP system for recycling companies (buying/selling metal and material scraps). Python/FastAPI backend + React/TypeScript frontend.

## Common Commands

### Backend (run from `/backend/`)

**IMPORTANTE: Usar `./venv/bin/` para todos los comandos Python.** No usar `pip`, `pytest`, `alembic` directamente — no estan en PATH.

```bash
./venv/bin/pip install -r requirements.txt
./venv/bin/uvicorn app.main:app --reload --port 8000
./venv/bin/alembic upgrade head
./venv/bin/alembic revision --autogenerate -m "description"

# Tests (requires test PostgreSQL on port 5433)
./venv/bin/pytest
./venv/bin/pytest tests/test_api_purchases.py -xvs                                          # single file
./venv/bin/pytest tests/test_api_purchases.py::TestPurchaseCreation::test_create_purchase -xvs  # single method
./venv/bin/pytest --cov=app --cov-report=html                                                # coverage
```

### Frontend (run from `/frontend/`)

```bash
npm install
npm run dev       # Dev server on port 5173
npm run build     # Production build
npm run lint      # ESLint
```

### Database

**3 entornos separados — NUNCA conectar desarrollo a produccion.**

| Entorno | Contenedor | Puerto | User | Password | DB | docker-compose |
|---------|-----------|--------|------|----------|-----|----------------|
| Desarrollo | reciclaje_dev_db | 5434 | admin | localdev123 | reciclaje_db | `docker-compose.yml` (raiz) |
| Tests | reciclaje-test-db | 5433 | admin | test_password | reciclaje_test | `backend/docker-compose.test.yml` |
| Produccion | VPS (76.13.118.195) | 5432 | — | — | — | SOLO via `/deploy` |

```bash
# Levantar BDs
POSTGRES_PASSWORD=localdev123 docker-compose up -d                         # Dev (port 5434)
cd backend && docker-compose -f docker-compose.test.yml up -d              # Test (port 5433)

# Migraciones (SOLO en dev y test, NUNCA produccion)
cd backend && ./venv/bin/alembic upgrade head                                                        # Dev (usa .env)
cd backend && DATABASE_URL=postgresql://admin:test_password@localhost:5433/reciclaje_test ./venv/bin/alembic upgrade head  # Test

# Replicar datos de produccion a dev local
cd backend && ./scripts/replicate_prod.sh
```

**REGLAS DE BASE DE DATOS:**
- **NUNCA** ejecutar queries, migraciones (`alembic upgrade`), ni scripts directamente contra la BD de produccion.
- **Migraciones** solo en desarrollo (5434) y test (5433). Produccion se migra automaticamente via `/deploy`.
- **Si necesitas datos reales**, primero replica produccion a local con `replicate_prod.sh`.
- **Despues de `replicate_prod.sh`, SIEMPRE re-correr `./venv/bin/alembic upgrade head`** si hay migraciones locales que prod aun no tiene — el DROP SCHEMA de la replica las borra y el backend local revienta con `UndefinedTable`.
- **`seed_test_data.py`** y cualquier script de carga apuntan a la BD configurada en `.env` (debe ser desarrollo).
- **Tests (`conftest.py`)** usan `TEST_DATABASE_URL` hardcoded (`admin:test_password@localhost:5433/reciclaje_test`). No dependen de `.env`.

## Architecture

### Backend (`backend/app/`)

Layered: **Endpoints → Services → Models**, with Pydantic schemas for validation.

- **`api/v1/endpoints/`** — Route handlers, one router per module.
- **`api/deps.py`** — DI: `get_db()`, `get_current_user()`, `get_required_org_context()`, `get_optional_org_context()`, `require_permission("module.action")` (AND), `require_any_permission("perm1", "perm2")` (OR). Admin bypasses all.
- **`services/`** — Business logic. All extend `CRUDBase` (pagination, soft delete, search, auto `organization_id` filtering).
- **`models/`** — SQLAlchemy 2.0. All domain models inherit `TimestampMixin` + `OrganizationMixin`.
- **`schemas/`** — Pydantic v2. Use `model_validate` with `from_attributes=True`.
- **`models/base.py`** — `GUID` type (cross-DB UUID), `Base`, mixins.
- **`core/`** — `config.py` (Settings from env), `security.py` (JWT + bcrypt), `database.py` (engine/session).

### Frontend (`frontend/src/`)

React 18 + TypeScript + Vite. Zustand (auth state), TanStack React Query + Axios (data), Tailwind + shadcn/ui (UI), lucide-react (icons), sonner (toasts).

- `services/api.ts` — Axios client: JWT auto-attach, X-Organization-ID from authStore, 401 redirect.
- `services/*.ts` — 20 service files. `hooks/use*.ts` — 17 React Query hooks with toast on mutations.
- `types/*.ts` — 20 type files matching backend schemas.
- `components/shared/` — DataTable, PageHeader, StatusBadge, MoneyDisplay, MoneyInput, DateRangePicker, SearchInput, ConfirmDialog, EmptyState, EntitySelect, WarningsList, PriceSuggestion, KpiCard.
- `components/auth/` — ProtectedRoute, OrganizationSelector, PermissionGate (wraps content by permission check).
- `pages/` — 75 page components. Forms use useState pattern. Admin pages: RolesPage, RoleEditPage, UsersPage.
- `utils/queryInvalidation.ts` — Centralized cache invalidation (see decision #26).

**Query Key Convention:** `['module', 'list'|'detail', filters|id]` (ej: `['purchases', 'list', filters]`, `['inventory', 'stock', params]`)

### Key Patterns

- **Multi-tenancy**: `X-Organization-ID` header required. `CRUDBase._base_query()` enforces `organization_id` filtering.
- **Soft delete**: `is_active` flag, never hard delete.
- **Purchase workflow (3-step)**: register (stock→transit, zero financial effect) → liquidate (confirm prices, avg cost, supplier balance, transit→liquidated) → pay (separate MoneyMovement).
- **Sale workflow (2-step)**: register (stock out, zero financial, COGS provisional) → liquidate (prices, COGS finalizado al promedio vigente #64, customer balance, commissions). Collect via MoneyMovement.
- **Double Entry (Pasa Mano)**: 2-step, NO inventory movements. Only third-party balances + commissions.
- **Liquidacion ≠ Pago**: Liquidation confirms prices/balances. Payment is always a separate MoneyMovement.
- **Moving average cost**: Recalculated at purchase LIQUIDATION (y reingreso ponderado al cancelar venta liquidada, #65). Oversell (pool negativo) conserva valor via `incorporate_into_pool` → `cost_adjustment` al P&L. Reversiones (cancelar compra liquidada, anular ajuste/transformación) por **remoción/reingreso ponderado** (`remove_from_pool`, #66) — nunca bloquean, conservan valor por construcción. `MaterialCostHistory` es **append-only** (8 source_types: 5 operativos + 3 de reversión); las reversiones escriben su registro, no borran el original.
- **Stock separation**: `current_stock_transit` (registered) vs `current_stock_liquidated` (confirmed). `current_stock` = total.
- **Negative stock allowed**: Sales, adjustments, transformations return `warnings[]` instead of blocking.
- **Sequential numbering**: Purchase/sale/double-entry numbers auto-incremented per org.
- **Treasury**: `MoneyMovement` with 25 types. Some have `account_id=NULL` (accruals, profit_distribution). Transfers = linked pair. Status: `confirmed|annulled`.
- **Provisions**: ThirdParty with `is_provision=True`. Negative balance = funds available. `provision_deposit` (account→provision), `provision_expense` (provision only, no account).
- **Price lists**: Append-only. Current price = most recent by `created_at`. Frontend auto-fills via `usePriceSuggestions()`.
- **Per-warehouse stock**: On-the-fly `SUM(inventory_movements.quantity) GROUP BY warehouse_id`.
- **BusinessDate**: All business dates normalized to noon UTC (12:00) via Pydantic `BeforeValidator` in `app/utils/dates.py`. Prevents timezone display issues.
- **RBAC**: `require_permission()` (AND) and `require_any_permission()` (OR) on all ~161 business endpoints. 72 permissions across 11 modules. 5 system roles: admin, bascula, liquidador, planillador, viewer. Custom roles via CRUD. **Master+Granular logic**: master permission (e.g., `treasury.view`) gives access to ALL sub-tabs; granular permissions (e.g., `treasury.view_provisions`) give access to specific sub-tabs WITHOUT master. Frontend: `usePermissions()` hook, `PermissionGate` component, sidebar filtering. Admin UI: RolesPage, RoleEditPage, UsersPage. **Superuser**: bypasses membership check in deps.py, gets synthesized admin context with all permissions. `/system/` endpoints use separate `get_current_superuser` guard.

### Business Modules

| Module | Endpoints | Key Features |
|--------|-----------|-------------|
| Auth | `/api/v1/auth/` | JWT login, registration, change password |
| Organizations | `/api/v1/organizations/` | CRUD + members with role_id FK |
| Roles & Permissions | `/api/v1/roles/` | 72 permissions, 11 modules, 5 system roles, custom roles, admin UI |
| Materials | `/api/v1/materials/` | CRUD + categories + business units, stock tracking |
| Third Parties | `/api/v1/third-parties/` | Category-based roles via behavior_type (material_supplier/service_provider/customer/investor/generic/provision/liability), balance tracking |
| Third Party Categories | `/api/v1/third-party-categories/` | Hierarchical categories (max 2 levels), behavior_type enum, CRUD + flat list |
| Purchases | `/api/v1/purchases/` | 3-step workflow, full edit (revert-and-reapply), auto-liquidate, immediate payment |
| Sales | `/api/v1/sales/` | 2-step workflow, commissions, received_quantity, full edit, immediate collection |
| Double Entries | `/api/v1/double-entries/` | 2-step, edit registered, price adjustments at liquidation, commissions |
| Treasury | `/api/v1/money-movements/`, `/api/v1/profit-distributions/` | 25 types, annulment with audit, unified account statement, evidence upload, PDF/Excel export, profit distribution |
| Fixed Assets | `/api/v1/fixed-assets/` | Monthly depreciation, apply-pending batch, dispose, revaluation up/down with account or third-party counterpart (#67), pay from account OR credit from supplier |
| Inventory | `/api/v1/inventory/` | Stock, movements, adjustments (4 types), transformations (3 cost methods), transfers, transit, valuation |
| Scheduled Expenses | `/api/v1/deferred-expenses/` | Deferred expenses with monthly installments |
| Reports | `/api/v1/reports/` | Dashboard, P&L, Cash Flow, Balance Sheet, Purchase/Sales/Margin reports, Treasury Dashboard |
| Config | Various | Warehouses, Money Accounts, Business Units, Expense Categories (direct/indirect), Price Lists, Third Party Categories |
| System (Super Admin) | `/api/v1/system/` | CRUD orgs, list users, add user to org. `get_current_superuser` guard. Org selector + system mode |

### Testing

PostgreSQL on port 5433. `conftest.py` provides: `test_user`, `auth_headers`, `org_headers`, `db_session`. Async auto-enabled via pytest-asyncio. Current: 1020 tests (20 integration; 6 fallos pre-existentes conocidos: 5×405 en organizations/auth_with_org y `test_update_insufficient_stock_fails` — ver nota decisión #54).

### Database

PostgreSQL 16, Alembic migrations. UUIDs everywhere (`GUID` type). Decimals: 4 places for quantities (kg), 2 for money. `CASCADE` on FK delete.

### Comments and Language

Business logic comments/docstrings in **Spanish**. Technical names (classes, functions, SQL) in English.

---

## Decisiones de Diseno

Numeradas secuencialmente. Solo agregar al final con el siguiente numero.

1. **Doble Partida sin inventario**: Operaciones "Pasa Mano" NO tocan bodega — inflaria costo promedio. Solo afecta saldos de terceros y comisiones (`commission_accrual`).

2. **Estados uniformes**: Compras, Ventas y DPs usan `registered | liquidated | cancelled`. Compras/Ventas: CREATE→LIQUIDATE→PAY. DPs: REGISTER→LIQUIDATE.

3. **Stock en transito**: Compras registradas crean stock transit sin efectos financieros. Al liquidar: confirmar precios, recalcular costo promedio, actualizar saldo proveedor, transit→liquidated.

4. **Gastos directos vs indirectos**: `is_direct_expense=True` afecta costo del material (flete, pesaje). `False` = administrativo. Clave para rentabilidad.

5. **COGS directo**: `SUM(sale_lines.unit_cost × quantity)` captura costo promedio movil al momento de cada venta. Mas preciso que metodo tradicional en inventario perpetuo.

6. **P&L de Doble Partida**: Utilidad Pasa Mano como linea separada, NO incluida en Sales Revenue ni COGS.

7. **Cash Flow hibrido**: Combina liquidacion de compras/ventas (cambios a account.balance) + money_movements. Opening balance = balance actual - cambios desde date_from. Desglosado: ingresos incluyen advance_collections; egresos incluyen provision_deposits, deferred_fundings, advance_payments, asset_payments.

8. **Edicion con Revert-and-Reapply**: Compras/ventas `registered` (sin DP) editables completamente. Revierte efectos colaterales, elimina lineas/movimientos, re-aplica. Compras bloquean si stock insuficiente; ventas permiten negativo con warnings.

9. **Cancelacion con reversal de costo**: ~~Usa `MaterialCostHistory` para revertir costo promedio. BLOQUEA si hay operaciones posteriores sobre el mismo material.~~ **[SUPERSEDED por #66]**: ya no hay rewind ni bloqueo — las reversiones son remoción ponderada, nunca bloquean. Sigue vigente: COGS de ventas existentes NO se recalcula al cancelar una compra (refleja costo al momento de la venta).

10. **Precio sugerido**: Auto-fill desde lista de precios vigente (solo si campo vacio). Hint clickable "Lista: $ X" para restaurar.

11. **Anticipos**: `advance_payment` (account-, supplier+) y `advance_collection` (account+, customer-). Proveedor balance > 0 = nos debe. Cliente balance < 0 = le debemos. Se consumen automaticamente al liquidar operaciones futuras.

12. **Upload de evidencia**: Un archivo por MoneyMovement (imagen/PDF, max 5MB). Almacenamiento local `{UPLOAD_DIR}/evidence/{org_id}/`.

13. **Gastos diferidos (ScheduledExpense)**: Pago upfront (`deferred_funding`: account(-), third_party(+), NO P&L) + cuotas mensuales (`deferred_expense`: NO cuenta, third_party(-), SI P&L). Crea ThirdParty auto `[Prepago] {name}` con `is_system_entity=True`.

14. **Pasivos (expense_accrual)**: Causar gasto sin mover dinero — NO cuenta, third_party.balance(-), aparece en P&L. Pago via `payment_to_supplier`. ThirdParty con behavior_type `liability`.

15. **Pago de pasivo (liability_payment)**: Tipo frontend-only que mapea a `payment_to_supplier` en backend. `backendTypeMap` convierte antes de enviar. Backend `_validate_third_party` acepta `require_behavior=["material_supplier", "service_provider", "liability"]`.

16. **Estado de cuenta unificado**: `GET /money-movements/third-party/{id}` fusiona TODAS las fuentes: MoneyMovements, compras/ventas standalone, comisiones, DPs. Ordenado por (transaction_date, sort_datetime, sort_key: 0=comercial, 1=tesoreria, 2=cancelacion). Desde #61 los eventos comerciales se posicionan en `liquidated_at` (fecha canónica #42) con `document_date` para mostrar ambas fechas. Default: ultimos 90 dias. Excluye compras/ventas con `double_entry_id IS NOT NULL`.

17. **Transformaciones de material**: Desarmado de compuestos. 3 metodos de costo: `average_cost` (default, usa costo promedio destino, genera `value_difference`), `proportional_weight`, `manual`. Validacion: `sum(dest_quantities) + waste == source_quantity` (solo si origen y destinos comparten unidad — ver #53). P&L: "Ganancia/Perdida por Transformaciones".

18. **Cantidad recibida (diferencia de bascula)**: `SaleLine.received_quantity` registra lo que el cliente peso. Si se envia, `total_price = received_quantity × unit_price`. COGS no cambia (usa cantidad original). Inventario no se ajusta — diferencia solo financiera.

19. **Balance Sheet extendido**: Activos: `provision_funds` (balance < 0), `prepaid_expenses` (system_entity, balance > 0), `fixed_assets` (SUM current_value). Pasivos: `liability_debt` (behavior_type `liability`, balance < 0). Signo: raw = "Saldo Contable", abs() = "Fondos Disponibles".

20. **Pago/Cobro inmediato**: Al liquidar o crear con auto_liquidate, opcionalmente crear MoneyMovement atomico en misma transaccion via `_create_movement()` (composable, usa flush). Schema validators con dependencias cruzadas.

21. **Activos Fijos**: Depreciacion mensual linea recta. Dos fuentes de pago (XOR): `source_account_id` (asset_payment) o `supplier_id` (asset_purchase, no afecta cuenta). `depreciation_expense` sin cuenta ni tercero, solo expense_category. Ultima cuota ajustada al residual. Dispose = depreciacion acelerada.

22. **P&L desagregado por fuente**: `ExpenseCategoryBreakdown.source_type` (expense, provision_expense, expense_accrual, deferred_expense, depreciation_expense). Frontend agrupa con subtotales por fuente.

23. **Comision causada (commission_accrual)**: Registra comisiones en P&L al liquidar (base devengado). account_id=NULL, third_party.balance(+). Auto-creada y auto-anulada. `sales_with_accrual` set en estado de cuenta evita duplicacion.

24. **Serializacion de fechas date→datetime**: `DoubleEntryResponse.date` (python `date`) necesita `field_serializer` a datetime mediodia UTC. Sin esto, JS parsea "2026-03-12" como midnight UTC → dia anterior en Colombia.

25. **RBAC backend**: 3 tablas: `permissions` (71, 11 modulos), `roles` (por org, `is_system_role`), `role_permissions` (M:N). `require_permission()` (AND) y `require_any_permission()` (OR) en ~161 endpoints. Admin bypassa todo.

26. **RBAC frontend + Admin UI**: `usePermissions()` hook (staleTime 5min), `PermissionGate` component, sidebar filtering, route protection. Master+Granular: master da acceso a todos los sub-tabs, granular da acceso sin master (OR logic). Admin UI: RolesPage, RoleEditPage (72 permisos por modulo), UsersPage. Al editar permisos se invalida `["permissions", orgId]`.

27. **Cache invalidation centralizada**: `queryInvalidation.ts`. Regla: si una operacion crea side-effects cross-module, invalidar TODOS los query keys afectados:
    - `invalidateAfterPurchase` (crear/editar): purchases + inventory + materials
    - `invalidateAfterPurchaseLiquidateOrCancel`: + money-movements + treasury-dashboard + third-parties + third-party-categories + reports + money-accounts
    - `invalidateAfterSale` (crear/editar): sales + inventory + materials
    - `invalidateAfterSaleLiquidateOrCancel`: + money-movements + treasury-dashboard + third-parties + reports + money-accounts
    - `invalidateAfterDoubleEntry`: double-entries + purchases + sales + third-parties + reports + money-accounts
    - `invalidateAfterTreasury`: money-movements + money-accounts + third-parties + reports + treasury-dashboard + scheduled-expenses
    - `invalidateAfterInventoryChange`: inventory + materials
    - Role CRUD: roles + permissions (si afecta rol del usuario actual)

28. **Scripts utilitarios**: `scripts/seed_test_data.py --clear` (solo maestros: materiales, categorias, bodegas, cuentas con saldo $0. NO crea terceros ni movimientos — flujo manual). `scripts/migrate_org.py` + `scripts/generate_migration_template.py` + `data/migration_template.xlsx` (migracion generica de cliente nuevo → EcoBalance: crea org via `/system/organizations`, carga 12 hojas, siembra stock via `POST /inventory/adjustments/increase` con `reason="Carga inicial migracion <Org>"` — este reason es el marcador que excluye los seeds del `adjustment_net` en `_calculate_profit` para que no inflen la Utilidad Acumulada: el inventario inicial es patrimonio absorbido por los socios, no ganancia operativa; fixed assets con `historical_load=True` cuando proveedor y cuenta vacios; `--dry-run` valida Excel offline sin login; `--apply` requiere superuser; verificacion automatica con tolerancia configurable; `--reset-org` para reset pre go-live). Si el corte tiene operaciones del dia que no estan en el snapshot del ERP origen (ej: arriendo del 11 de mayo), correr con `--balance-tolerance` alto y registrar el movimiento via UI inmediatamente despues del `--apply`. Ver `scripts/MIGRATION.md`. **Skill `/migrate-client`** orquesta el flujo seguro extremo-a-extremo: recolecta inputs via AskUserQuestion, hace pre-flight (Excel + backend + alembic), dry-run dev, apply dev con `--reset-org`, **confirmacion 1**, dry-run prod, **confirmacion 2 critica**, apply prod sin `--reset-org`, cleanup opcional, reporte final. La skill NUNCA pasa `--reset-org` a prod y NUNCA corre `alembic upgrade` (eso es de `/deploy`).

29. **Super Admin + Multi-Org**: `is_superuser=True` bypasses membership en `get_required_org_context()` / `get_optional_org_context()` (sintetiza admin context con todos los permisos). `/system/` endpoints (6) protegidos con `get_current_superuser`. Frontend: `organizationId="system"` sentinel — API interceptor no envia `X-Organization-ID`, `usePermissions` retorna admin full, sidebar muestra solo seccion SISTEMA, ProtectedRoute permite pasar sin org. Header dropdown: multi-org selector + opcion "Sistema" para superusers. `queryClient.clear()` al cambiar org. Soft delete de org: `is_active=False` + desactivar usuarios huerfanos.

30. **Comision de compra (prorrateo al costo)**: `PurchaseCommission` tabla separada (reutiliza enum `commission_type`). A diferencia de ventas (comision→P&L via `commission_accrual`), en compras la comision se **prorratea al costo** del material: `adjusted_unit_cost = unit_price + (commission_prorate / quantity)`. Afecta `InventoryMovement.unit_cost` y costo promedio. NO crea MoneyMovement — solo actualiza `third_party.balance` del comisionista. Cancelacion revierte balance. UI en Create/Edit/Liquidate/Detail pages.

31. **Balance Detallado y General unificados**: Ambos reportes usan `_classify_third_party` para clasificar cada tercero en **una sola sección** por prioridad (evita doble conteo cuando un tercero tiene múltiples behavior_types). Balance General refactorizado: 12 queries SQL de terceros reemplazadas por 1 query + iteración con clasificación. Secciones activo: cash, inventory_liquidated, customers_receivable, supplier_advances, service_provider_advances, liability_advances, investor_receivable, provision_funds, prepaid_expenses, generic_receivable, fixed_assets. Secciones pasivo: suppliers_payable, service_provider_payable, liability_debt, investors_partners, investors_obligations, investors_legacy, customer_advances, provision_obligations, generic_payable. `investor_type`: `"socio"` → partners, `"obligacion_financiera"` → obligations, null → legacy.

32. **Comisionista requiere service_provider**: Validación en `_process_commissions` (compras, ventas) y `_create_commission_records` (DPs): el recipient debe tener `behavior_type` `service_provider` via `has_behavior_type()`. Razón: comisiones generan balance negativo; sin behavior_type proveedor, `_classify_third_party` no lo ubica en pasivos. Frontend: selector de comisionistas filtrado a `payable-providers` (solo `service_provider`). Reglas de proveedor por contexto: DP supplier = `material_supplier` only. Fixed asset supplier = cualquiera excepto `provision`/`liability`. Purchase supplier = `material_supplier`. Sale customer = `customer`.

33. **Categorías de terceros (behavior_type)**: Eliminados flags booleanos. Unica fuente de verdad: `ThirdPartyCategory` con `behavior_type` enum (7 tipos: `material_supplier`, `service_provider`, `customer`, `investor`, `generic`, `provision`, `liability`). M:N via `ThirdPartyCategoryAssignment`. Jerarquía max 2 niveles. `_classify_third_party()` en reports usa behavior_types + category_names. Endpoints: `/suppliers` filtra `material_supplier`, `/payable-providers` filtra `service_provider`, `/liabilities` filtra `liability`, `/investors` filtra `investor`. `liability` y `provision` ocultos de Config y multi-select (se crean solo desde LiabilitiesPage/ProvisionsPage). `payment_to_supplier` y `advance_payment` aceptan `liability` en `require_behavior`.

34. **Repartición de utilidades (profit_distribution)**: Causado contable — incrementa deuda a socios (`third_party.balance -= amount`) sin mover dinero de cuentas (`account_id=NULL`). MoneyMovement tipo `profit_distribution`. NO afecta P&L ni Cash Flow. Balance Sheet: `accumulated_profit` (P&L all-time via `_calculate_profit()` refactorizado) y `distributed_profit` (SUM líneas). Retiro físico usa `capital_return` existente. Permiso: `treasury.manage_distributions`. 18 tests.

35. **Precios modo tabla (editable spreadsheet)**: `GET /price-lists/table` retorna todos los materiales activos con precio vigente (DISTINCT ON + LEFT JOIN). Frontend: tabla manual con celdas editables (click→input, Enter/blur→auto-save via `POST /price-lists` existente, Escape→cancel). Check verde 1.5s en save exitoso. Filtro categoría (server-side), búsqueda código/nombre (client-side). Modal historial por material. Permission-gated: `materials.edit_prices` para editar.

36. **Subcategorías de gasto (max 2 niveles)**: `parent_id` FK self-referencial en `expense_categories`. Validaciones: max 2 niveles (parent no puede tener parent), misma org, activo. Subcategoría hereda `is_direct_expense` del padre. `GET /flat` retorna lista plana con `display_name` ("PADRE > HIJO"), ordenada alfabéticamente. Frontend: selector padre en dialog config (oculta switch is_direct si tiene padre), 5 formularios treasury usan `useExpenseCategoriesFlat()` + `display_name` como label. 13 tests.

37. **Frontend categorías de terceros**: `ThirdPartyResponse.categories[]` con `display_name` y `behavior_type`. ThirdPartyFormDialog: multi-select checkbox agrupado por behavior_type, oculta `liability` y `provision`, `initial_balance` siempre 0 (balances solo via transacciones). ThirdPartiesPage: 8 tabs (Todos, Proveedores, Servicios, Clientes, Inversionistas, Pasivos, Provisiones, Genéricos). Config: 7 behavior_types en tabla, dropdown oculta `liability`/`provision`. MovementCreatePage: `liability_payment` y `expense_accrual` usan `useLiabilities()`. LiabilitiesPage filtra categorías `"liability"`.

38. **Balance Detallado: liability separado + sub-agrupación por categoría**: (a) `_classify_third_party()` separa `liability` de `service_provider` en secciones propias: `liability_advances` (activo, bal>0), `liability_debt` (pasivo, bal<0). `service_provider_payable` reemplaza `liabilities_other`. (b) Sub-agrupación opcional por `ThirdPartyCategory` en todas las secciones de terceros. `_load_tp_behavior_map()` retorna 3er dict `tp_cat_by_behavior[tp_id][behavior_type] = display_name`. `_group_by_category()` agrupa items si >1 categoría distinta; orden por total desc, "Sin Categoría" al final. Frontend: 2 niveles expand/collapse con keys `"section_key"` y `"section_key:group_label"`. Excel export con sub-grupos. `BalanceDetailedGroup` schema nuevo.

39. **Edicion de clasificacion de gastos**: `PATCH /money-movements/{id}/classification` permite cambiar `expense_category_id` y asignacion UN en movimientos tipo gasto (`expense`, `expense_accrual`, `provision_expense`, `deferred_expense`, `depreciation_expense`). No modifica montos/cuentas/terceros. Permiso: `treasury.edit_classification` (admin + liquidador). Frontend: boton "Editar Clasificacion" en `MovementDetailPage` + modal `EditClassificationModal` reutiliza `BusinessUnitAllocationSelector`. No afecta saldos — solo impacta reportes (P&L por categoria, Rentabilidad por UN).

40. **Transformation bloquea cancelacion de compra fuente**: **[SUPERSEDED por #66]** — el bloqueo protegía contra el rewind, que ya no existe: cancelar compra de material transformado ahora pasa con remoción ponderada (conserva por construcción) + warning PR-3. El registro MCH `transformation_out` se mantiene (auditoría + valuación as-of históricas).

42. **Fecha de liquidacion personalizada**: Al liquidar compra, venta o doble partida, se puede pasar `liquidation_date` (backend por defecto: fecha del documento; UI de liquidación 2-pasos SIN default desde #62). Validaciones: no anterior al documento, no futura. `liquidated_at = liquidation_date or document.date` — fecha canónica de cuándo la operación tiene efecto financiero. Pago/cobro inmediato usa `liquidated_at`, no `document.date`. Todos los reportes (P&L, purchase/sale report, margin analysis, dashboard, profitability by BU, balance sheet) filtran por `liquidated_at`. Excepción aceptada: `daily_trend` dentro de los gráficos agrupa por `Purchase/Sale.date` (fecha documento) — los totales del período son correctos, solo la distribución diaria interna puede diferir en edge cases.

43. **Backfill `liquidated_at` = `date` (script one-shot)**: `backend/scripts/backfill_liquidated_at.py` corrige datos históricos de compras/ventas/DPs cuyo `liquidated_at` quedó seteado con `now()` antes del commit `bdc0791` (2026-04-13). Además corrige `MoneyMovement.date` de `payment_to_supplier` / `collection_from_client` creados automáticamente al liquidar antes de `e892e81`. Flags: `--dry-run` (default), `--apply`, `--cutoff-liquidated`, `--cutoff-mm`. Idempotente (filtros WHERE `liquidated_at::date != date::date AND liquidated_at < cutoff`). Crea tabla `backfill_liquidated_at_audit` con snapshot `(entity_table, entity_id, field, old_value, new_value)` para rollback selectivo. Aplicado en dev 2026-04-13: 639 filas (302 purchases + 149 sales + 93 DPs + 78 payment_to_supplier + 17 collection_from_client). Prod: solo tras backup completo + dry-run + aprobación del cliente.

41. **Balance histórico por fecha de corte (as_of_date)**: `GET /reports/balance-sheet?as_of_date=YYYY-MM-DD` y `/balance-detailed?as_of_date=...` calculan un snapshot exacto a esa fecha. Sin parámetro = comportamiento actual (fast path). Implementación: 5 helpers históricos (`_get_account_balances_as_of`, `_get_tp_balances_as_of` con 5 fuentes, `_get_inventory_as_of`, `_get_fixed_assets_as_of`, `_get_distributed_profit_as_of`). `_active_at_cutoff()` excluye canceladas/anuladas SIN importar fecha — se tratan como "nunca existieron" (simplificado deliberadamente en `735c2c3`; el diseño original sí las incluía post-corte). Consecuencia: cancelar/anular una operación reescribe balances históricos ya consultados. `MaterialCostHistory.transaction_date` (migración `e3f4g5h6i7j8`) guarda la fecha de negocio para costo promedio histórico exacto. `_calculate_profit` acepta `cutoff_dt` para P&L acumulado. Frontend: date picker compartido via `dateFilterStore.balanceAsOfDate` — persiste entre Balance General y Detallado. `_get_inventory_as_of` usa 3 niveles de fallback para costo: (1) último `MaterialCostHistory` con `transaction_date <= corte`, (2) `previous_cost` del primer registro posterior, (3) `unit_cost` del último `InventoryMovement` de compra/ajuste antes del corte — cubre ajustes legacy sin historial de costo. `_get_fixed_assets_as_of` y `_get_fixed_assets_detailed_as_of` usan `AssetDepreciation.period` ("YYYY-MM") como fecha contable, no `applied_at`. Helper `_fa_value_at_cutoff`: (1) si hay depreciación con `period <= corte` → usa `current_value_after`; (2) si no (activos cargados con depreciación acumulada histórica pre-sistema) → reconstruye como `fa.current_value + SUM(amount de períodos posteriores al corte)`, revirtiendo exactamente lo aplicado después del corte.

44. **Reporte de Gastos con agrupación flexible UN/Categoría**: `GET /reports/expenses` y `/reports/expenses/detail` permiten ver todos los gastos del periodo agrupados por UN, Categoría, o ambas en cualquier orden (`group_by` ∈ `bu | category | bu_then_category | category_then_bu`). Reusa `EXPENSE_MOVEMENT_TYPES` y la lógica 3-tier de Rentabilidad por UN (`_compute_expense_allocations` replica exactamente líneas 3196-3226 de `get_profitability_by_business_unit`, incluyendo el edge case legacy `is_direct_expense=True + UN NULL → unassigned directo`). Filtros: `business_unit_id[]`, `expense_category_id[]` (padre expande automáticamente a hijas, aplicado **después** del prorrateo para preservar proporcionalidad). Endpoint detail acepta flags `business_unit_unassigned` y `category_uncategorized` para drill-down al bucket "Sin Asignar"/"Sin Categoría". Cada item del detail expone `allocated_amount` (porción asignada al grupo) y `allocation_type` (`direct|shared|general`). Permiso `reports.view_expenses` (asignado a viewer + liquidador, consistente con migración `e7f8a9b0c1d2`). Frontend: `ExpensesReportPage` con tabla recursiva expandible, modal de drill-down con link a `/treasury/:id?returnTo=...` para editar movimientos. El modal de drill-down (`ExpenseDetailSheet`) y el modo "Sin agrupar (detalle)" tienen botón "Excel" → `exportExpensesFlatExcel(data, from, to, context?)`: exporta Fecha/#/Tipo/Tercero/UN/Categoría/Descripción/**Monto Original**/**Asignado** (ambas numéricas sumables vía `applyCurrencyFormat`)/Asignación; el param opcional `context` (`{buLabel, catLabel}`) agrega esas líneas al header para que el archivo sea autocontenido cuando se exporta desde el drill-down de un grupo. 12 tests (incluyendo paridad con P&L y con Rentabilidad por UN, edge case legacy, filtro padre→hijas, RBAC).

45. **Filtro Pasa Mano en Compras/Ventas (`dp_filter`)**: `GET /reports/purchases` y `/reports/sales` aceptan `dp_filter` ∈ `all | exclude | only` (default `all` = comportamiento previo). Aplica `Purchase.double_entry_id IS [NOT] NULL` (resp. `Sale`) tanto a `base_filters` como a `line_filters` para que las 4 queries (totales, by_supplier/customer, by_material, daily_trend) sean consistentes. UI: dropdown "Operaciones" en `PurchaseReportPage` y `SalesReportPage` con persistencia en URL (`?dp=exclude|only`, omitido cuando `all`). El label seleccionado se pasa al Excel export para que el header del archivo sea autocontenido. Margin Analysis y P&L ya separan DPs por su cuenta (decisiones #6, #16) — quedan fuera del alcance. 6 tests nuevos: `test_purchase_report_dp_filter_{all,exclude,only}` y análogos para sales.

46. **Carga histórica de activos fijos (`historical_load`)**: `POST /fixed-assets/` acepta `accumulated_depreciation: Decimal` y `historical_load: bool` para cargar activos que ya venían depreciados desde otro ERP (migración de organización). Si `historical_load=True`: no se valida `source_account_id`/`supplier_id` (deben ser `None` — el pago ya ocurrió en el sistema origen), no se crea `MoneyMovement` de compra, no se mueven balances de cuenta ni proveedor; `current_value = purchase_value - accumulated_depreciation`. Validación adicional: `accumulated_depreciation ≤ purchase_value - salvage_value`. El flag NO se persiste — la firma "carga histórica" se infiere de `accumulated_depreciation > 0 AND purchase_movement_id IS NULL`. La deuda pendiente al proveedor (si el activo no se pagó del todo) se modela vía `initial_balance` del `ThirdParty` al crearlo, no acá. El fallback de `_fa_value_at_cutoff` (decisión #41) ya cubre activos sin records `AssetDepreciation` — funciona out of the box para Balance Detallado histórico. 5 tests nuevos en `TestFixedAssetHistoricalLoad`.

47. **Seed default `Socios` (era `Inversionista`)**: El módulo de repartición de utilidades filtra socios por `ThirdPartyCategory.name ILIKE '%socio%'` (decisión #34). El seed default de orgs nuevas en `services/organization.py` creaba la categoría como `"Inversionista"` — orgs nacían sin poder repartir utilidades hasta renombrar manualmente. Cambiado a `"Socios"` para alinear con el filtro. Migración `3acfc5ab3d68` renombra `Inversionista → Socios` idempotente (solo si la org no tiene ya una categoría `Socios`). Compatible con `_classify_third_party`: "Socios".lower() contiene `"socio"` → bucket `investors_partners`. NO afecta el behavior_type enum (sigue siendo `investor`) ni los labels de UI/reports (siguen mostrando "Inversionistas" como nombre de la sección agrupada). Script `migrate_metarecycling.py` y template Excel actualizados.

48. **Anulación de `ProfitDistribution`**: La entidad agregada `ProfitDistribution` ahora persiste `status` (`confirmed | annulled`) + `annulled_reason / annulled_at / annulled_by`. Antes solo se podían anular los `MoneyMovement` hijos individualmente, lo que dejaba la distribución colgada en el historial y contando en `calculate_distributed_profit`. Endpoint `POST /profit-distributions/{id}/annul` (permiso `treasury.manage_distributions`) hace cascade outbound anulando todos los MMs asociados vía `money_movement.annul()`. Adicionalmente, `money_movement.annul()` detecta cuando el MM era tipo `profit_distribution` y, si la distribución padre ya no tiene ningún MM activo, marca automáticamente la distribución como anulada (cascade inbound) con reason "Auto-anulada (MM individual anulado): {reason}". `calculate_distributed_profit` ahora filtra por `status='confirmed'` para que las distribuciones anuladas no inflen `distributed_profit`. `_get_distributed_profit_as_of` (decisión #41) ya filtraba por MM.status, queda intacto. Migración `9cff249f95cf` agrega columnas + backfill: marca como `annulled` las distribuciones cuyos MMs ya estaban todos anulados (caso reportado por cliente).

49. **Drill-down preciso en P&L (Estado de Resultados)**: 15 líneas del P&L son `<Link>` clickeables a páginas existentes (`/sales`, `/double-entries`, `/treasury`, `/inventory/adjustments`, `/inventory/transformations`) con filtros precisos en URL. Promesa contractual: **la suma de los items del listado destino = el número del P&L (tolerancia $1)**. Test de oro `tests/test_pnl_drilldown_parity.py` (16 tests, uno por línea) es el guardrail — si alguno falla, la promesa se rompe. Cuatro filtros nuevos cross-módulo permiten replicar exactamente la lógica de `_calculate_profit`:
    - **`date_field` intercambiable** en `/sales` y `/double-entries` (`date` default | `liquidated_at`): el P&L filtra por `liquidated_at` (decisión #42), pero los listados filtran por `date` por default. Sin este param, una venta registrada en M y liquidada en M+1 aparecería en M al hacer drill-down pero contaría en M+1 en el P&L. Precedente para futuros drill-downs (P&L de compras, etc.).
    - **`dp_filter`** en `/sales` (`all` default | `exclude` | `only`): mismo patrón que decisión #45 para reports — Ventas Standalone (excluye DPs) vs solo DPs. P&L usa `exclude` para "Ingresos por Ventas".
    - **`adjustment_class`** (`gain | loss`) + **`movement_type` CSV** en `/money-movements`: separa ganancia/pérdida por ajuste de terceros. CSV permite consultar `tp_adjustment_credit,tp_adjustment_debit` simultáneamente vía `IN (...)`.
    - **`exclude_migration_seeds`** en `/inventory/adjustments`: filtra ajustes con `reason ILIKE 'Carga inicial migracion%'` (marker de decisión #28). P&L los excluye porque representan patrimonio inicial absorbido por los socios, no ganancia operativa.

    UI: helper `DrillRow` (ChevronRight opacity-0 group-hover) en `ProfitAndLossPage`. `TreasuryPage` gana 7 tabs (`service_income`, `provision_expense`, `expense_accrual`, `deferred_expense`, `depreciation_expense`, `commission_accrual`, `tp_adjustment` compuesto), lee `?status=` y `?adjustment_class=` de URL. Tab compuesto `tp_adjustment` mapea internamente a CSV. `TransformationsPage` gana 2 columnas: `value_difference` (color emerald/red por signo) y `waste_value`. `SalesPage` gana columna "Costo (COGS)" derivada (`total_amount - total_profit`, sin schema change) cuando los filtros del drill-down están activos. Badges informativos en cada página destino (`Sin Pasa Mano`, `Por fecha de liquidación`, `Sin seeds de migración`, etc.) muestran filtros activos con botón × para limpiar. `useDateFilter` (Zustand+persist) propaga el rango de fechas automáticamente. Páginas con tabs en URL unificadas a `?tab=` (consistencia). `transfer_in` queda fuera de scope (no aporta al P&L). El `status=confirmed` explícito en URLs de `/treasury` es necesario porque el endpoint `/money-movements` muestra confirmed+annulled por default pero `_calculate_profit` solo cuenta confirmed.

50. **P&L Mensual (comparativo mes a mes con cutoff personalizado)**: Nuevo tab "Mensual" dentro de `/reports/profit-and-loss` (view persistido en URL `?view=monthly`, fallback silencioso a `period` si valor inválido). Una columna por **mes contable** derivado de un `cutoff_day` configurable (1–28, default 1): un mes contable empieza el día `cutoff_day` y termina el día `cutoff_day - 1` del mes siguiente. Con `cutoff_day=1` produce meses calendario; con `cutoff_day=4` produce columnas tipo "4 Mar – 3 Abr 2026". Incluye todos los meses que **intersectan** el rango (sin snap). Cada celda numérica es `<Link>` al listado destino con `date_from`/`date_to` del mes contable — mismas URLs que el tab "Periodo" (decisión #49). Columna `Total` (con border-left) calculada con `_calculate_profit(date_from=range_from, date_to=range_to)` directamente — paridad exacta con tab "Periodo", **NO** suma aritmética de las columnas (los bordes parciales recortarían meses contables).

    Backend: `GET /reports/profit-and-loss/monthly?date_from&date_to&cutoff_day` retorna `ProfitAndLossMonthlyResponse { cutoff_day, range_from, range_to, periods: ProfitAndLossMonthlyPeriod[], totals: ProfitAndLossResponse }`. `ProfitAndLossMonthlyPeriod` extiende `ProfitAndLossResponse` agregando `label`. Helper `_split_into_accounting_months(date_from, date_to, cutoff_day) -> list[tuple[date, date, str]]` genera columnas (year-disambiguation: cuando cross-year usa `"15 Dic 2025 – 14 Ene 2026"`, mismo año usa `"4 Mar – 3 Abr 2026"`; `cutoff_day=1` siempre usa `"Mar 2026"`). Service reusa `get_profit_and_loss` por columna para garantizar paridad campo por campo. Permiso: `reports.view` o `reports.view_pnl` (mismo que P&L Periodo).

    **Cap defensivo de 24 meses** (~168 SQL queries peor caso, < 4s): rangos que generan más → 422 con detail descriptivo. `cutoff_day` validado `ge=1, le=28` por Pydantic Query (29–31 no soportado por edge cases de febrero).

    **🔴 Prerequisito (Commit 1) — URL date params override en destination pages**: las 5 páginas destino del drill-down (`SalesPage`, `TreasuryPage`, `DoubleEntriesPage`, `AdjustmentsPage`, `TransformationsPage`) leían `dateFrom`/`dateTo` SOLO desde `useDateFilter` (Zustand+persist) — ignoraban `?date_from`/`?date_to` de URL. Sin el fix, los links de celda del tab Mensual abrirían el rango global, rompiendo la promesa "suma celda = suma listado". Patrón aplicado: `urlDateFrom = searchParams.get("date_from"); effectiveDateFrom = urlDateFrom || dateFrom; hasUrlDateOverride = Boolean(urlDateFrom || urlDateTo)`. El `DateRangePicker` opera sobre `effectiveDateFrom/effectiveDateTo`; cuando el usuario cambia el rango visualmente, **se limpian los URL params** (`clearDateOverride()`) para evitar incoherencia entre store y URL. Badge visual `Rango: X – Y [×]` (indigo) cuando hay override activo. ⚠️ **NO envolver en `useState`** — `searchParams.get("date_from")` se lee directo en cada render para que reaccione a cambios de URL. **Beneficio retroactivo a decisión #49**: ahora las URLs con `?date_from&date_to` son deep-linkables aunque el usuario cambie el rango global desde otra página. Bonus: `AdjustmentsPage` y `TransformationsPage` tenían un bug previo donde `Tabs.onValueChange` hacía `setSearchParams({ tab: v })` (wipe-out de TODOS los URL params) — refactorizado a functional updater preservando el resto de filtros.

    Frontend: refactor puro previo extrayó `ProfitAndLossPeriodView.tsx` (contenido idéntico al P&L original sin `ReportsLayout`). `ProfitAndLossPage.tsx` reescrito como wrapper con `<Tabs>` (`period` | `monthly`). `ProfitAndLossMonthlyView.tsx` nuevo: tabla pivotada con primera columna sticky (labels P&L con indentación para sub-líneas), `cutoff_day` como `<Input type="number">` persistido en URL, `DateRangePicker` compartido con tab Periodo via `useDateFilter`. Helper `withDateRange(base, from, to)` arma cada URL de drill-down con los bounds del mes. Excel export `exportPnlMonthlyExcel` (patrón `currencyColumns` de decisión #44: números como `number` no `string`, sumables en Excel, headers de mes = `period.label`, bold en subtotales, border-left en columna `Total`).

    **Test de oro guardrail**: `test_pnl_monthly_cell_drilldown_matches_listing` en `tests/test_pnl_monthly.py` valida que para cada mes contable y al menos 3 líneas clave (`sales_revenue`, `commissions_paid_sales`, `operating_expenses` por `source_type`), `sum(items_listado(period_from, period_to)) == periods[mes].<linea>` (tolerancia $1). Fixture incluye una venta cross-month (registrada feb 28, liquidada mar 3) para atrapar regresiones de boundary en `date_field=liquidated_at`. Análogo mensual del `test_pnl_drilldown_parity` (decisión #49). 10 tests nuevos en total (5 unit de `_split_into_accounting_months` cubriendo calendario, cutoff custom, parcial dentro de un mes, year rollover, invalid; 4 endpoint cubriendo periods_sum_to_total, RBAC 403, validación 422, cap 24 meses 422; 1 parity bloqueante).

51. **Filtros configurables en reportes (paridad web/PDF/Excel) + PDF mobile-friendly**: Helper `frontend/src/utils/balanceTransform.ts` aplica filtros (`hideBelow`, `sortBy: amount_desc | alphabetical`) sobre datos pre-fetched antes de renderizar — mismo helper alimenta web (`BalanceDetailedPage`), `exportBalanceDetailedPDF` y `exportBalanceDetailedExcel`. Garantiza paridad visual cross-format: lo que se ve en pantalla es lo que se descarga. Filtros persistidos en `localStorage` con key versionada (`balance_detailed_filters_v1`). Cuando un **grupo entero** cae bajo el umbral, `sectionExtraHidden(section)` computa el delta `section.hidden_count - Σ groups[].hidden_count` para que la nota a nivel sección no pierda items. Sort `amount_desc` es **signed-desc** (no por valor absoluto) — terceros con saldo negativo caen al final naturalmente, sin necesidad de excluirlos. Negativos como paréntesis `($X)` en los 3 formatos: web `fmtBalance`, PDF `fmtBal` interno, Excel `CURRENCY_FMT = '"$"#,##0;("$"#,##0)'` (mantiene valores como `number`, sumables/sortables).

    **PDF mobile/desktop dual format**: `exportAccountStatementPDF(data, orgName, format?: "mobile" | "desktop")` con default `mobile`. Selector persistido en `localStorage` key `account_statement_pdf_format_v1` en `AccountStatementPage` y `AccountMovementsPage` (dropdown `w-full sm:w-32`). **Por qué fuentes grandes en mobile**: iPhone renderiza A4 portrait (210mm) al ancho completo de pantalla (~390pt) — fuentes <10pt PDF se ven a ~5pt efectivos en pantalla, ilegibles. Mobile usa header 17pt → línea 1 (fecha · tipo · monto) 11pt → línea 2 (desc · saldo) 10pt. Layout 2 columnas fijas: izquierda info (fecha gris + tipo bold + desc truncate), derecha plata (`±monto` colored bold + `Saldo X` con label gris). `RIGHT_COL_WIDTH=80mm` reservados para acomodar montos en 11pt. Saldo solo en última línea de grupos agrupados (`lastByGroup` map). Desktop usa landscape con tabla original.

    **Helper `ellipsis(text, maxWidth)`** (closure interna en `exportAccountStatementPDF`): trunca por `doc.getTextWidth()` real iterativo (`text.slice(0, -1)` hasta caber con `…`), NO por `substring(0, N chars)`. Letras anchas (M, W) ya no causan overflow. Aplicado en mobile (descripción + tipo en columna izquierda contra `leftMaxWidth`) y desktop (tipo `colX.desc - colX.type - 2`, descripción `colX.debit - colX.desc - 28` con safety margin de 28mm para el monto right-aligned). Pattern reusable para futuros PDFs con columnas estrechas — preferir `ellipsis()` sobre `substring()` siempre que el texto venga del usuario.

52. **Excel de operaciones con hoja Detalle por línea**: Compras, Ventas y DPs exportan un workbook de **2 hojas**: "Resumen" (1 fila por operación, espeja la tabla en pantalla) + "Detalle" (1 fila por material con `Kg` como **número sumable**, no texto — para análisis y liquidación de comisiones a comerciales). El cambio es 100% frontend: los list endpoints ya devuelven `lines[]` completas (no hay `include_lines` param, siempre vienen). `DataTable` ganó prop opcional `exportOverride?: (rows) => void` (backwards-compatible) que reemplaza el export genérico basado en columnas por una función dedicada; el resto de listados no se tocan. Funciones en `excelExport.ts`: `exportPurchasesDetailExcel` / `exportSalesDetailExcel` / `exportDoubleEntriesDetailExcel`, cada una recibe los flags de permiso para omitir columnas de precio/utilidad (`view_prices` / `view_profit` / `view_values`, igual que en pantalla). `Kg` con formato `#,##0.####` vía helper local `applyNumberFormat`; montos como `number` con `CURRENCY_FMT`. Coerción `num()` defensiva por si el backend serializa Decimal como string. Respeta filtros activos y tope de 1000 filas (lógica existente de `onExportAll`). Sin info de comisiones (decisión del cliente). COGS en Resumen de ventas = `total_amount - total_profit` (mismo derivado que decisión #49). Estados en femenino vía `OP_STATUS_LABELS` (sirve compra/venta/DP).

53. **Balance de transformaciones unit-aware (cambio de unidad)**: La validación `sum(destinos) + merma == origen` (decisión #17) asume conservación de masa — **solo aplica cuando origen y todos los destinos comparten `Material.default_unit`** (ej: kg→kg). Cuando difieren (ej: desarmar "Aire Acondicionado" [`unidad`] en Hierro/Cobre [`kg`]), no hay invariante de cantidad: se consumen las unidades de origen y se producen libremente los kg destino. **El check se movió del schema Pydantic al servicio** (`MaterialTransformationService.create`) porque el schema no conoce las unidades de los materiales (no tiene DB); por eso un balance que no cuadra ahora retorna **400** (HTTPException del servicio), no 422 (validation error del schema). En modo cambio de unidad la **merma debe ser 0** — se modela como un material destino en la unidad destino (ej: "Basura" en kg); merma>0 → 400. Los 3 métodos de costo ya funcionaban cross-unit (operan sobre valores $, no sobre igualdad de cantidades; `proportional_weight` prorratea el valor del origen por peso de destinos, correcto en desarmado). Frontend (`TransformationCreatePage`): detecta cambio de unidad comparando `default_unit` de origen vs destinos, relaja el gate `balanceOk = isCrossUnit || isBalanced`, muestra la unidad en cada selector (`CODE - Nombre (unidad)`) y junto a cada cantidad, y reemplaza la card roja de balance por un panel informativo índigo (ocultando el campo Merma) cuando hay cambio de unidad. Consistente con la filosofía "avisar, no bloquear" (stock negativo). 3 tests: `test_create_balance_mismatch` (mismo-unidad → 400), `test_cross_unit_skips_balance` (unidad→kg desbalanceado → 201), `test_cross_unit_with_waste_rejected` (cambio de unidad + merma → 400).

54. **Unidad de medida dinámica en operaciones (`material_unit`)**: Las líneas de Compra/Venta/DP exponen `material_unit` (`PurchaseLineResponse`/`SaleLineResponse`/`DoubleEntryLineResponse`, default `"kg"`). Los 3 enrich (`_enrich_purchase/sale/double_entry_response`) lo llenan con `line.material.default_unit` — **cero queries nuevas**: `line.material` ya se accede para `code`/`name`. Principio: `formatWeight(value, unit="kg")` es unit-agnóstico, solo el label miente; fallback `"kg"` si null/vacío (preserva comportamiento previo). Frontend: detail/list/PDF/Excel usan `formatWeight(qty, line.material_unit)` y headers genéricos "Cantidad" (no "Cantidad (kg)") → soporta operaciones con líneas de **unidades mixtas**. Forms Create/Edit resuelven la unidad client-side vía `materials.find(...).default_unit` (helper local `unitSuffix`); Liquidate/Detail usan `line.material_unit` del backend (no cargan `materials`). Totales de diferencia de báscula muestran unidad solo si todas las líneas la comparten (sino ambiguo). Excel Detalle (#52): columna "Kg"→"Cantidad" (numérica sumable) + nueva columna "Unidad". **Limitaciones conocidas (fuera de alcance)**: (a) comisiones `per_kg` muestran "/kg" y la matemática suma `kg + unidades` en operaciones mixtas — `per_kg` sobre material por unidad es conceptualmente `$/unidad`; revisar si el modelo de comisiones lo requiere. (b) Inventario (Stock/Movimientos/Ajustes/Transformaciones-display) sigue mostrando "kg". (c) `MoneyInput decimals={2}` permite fracciones de unidad. 6 tests (assert `material_unit` "kg" en 3 create existentes + 3 nuevos con material "unidad"). **Nota**: `test_update_insufficient_stock_fails` (compras) falla pre-existente (update no bloquea con stock insuficiente, contradice decisión #8) — ajeno a este cambio.

55. **Fix Saldo Inicial del estado de cuenta = saldo de apertura de la ventana**: La fila sintética "Saldo Inicial" del estado de cuenta unificado (decisión #16) usaba `initial` (saldo base del tercero) en vez del **saldo de apertura real de la ventana de fechas**. Cuando el tercero tenía movimientos confirmados ANTES del rango visible (default: últimos 90 días) — ej. una `tp_transfer_in` de hace >90 días — esos movimientos no se sumaban a la fila inicial, produciendo un "salto" inexplicable: el saldo brincaba del Saldo Inicial al primer movimiento listado sin justificación visible (caso reportado: Eca Palmira, un cobro de $84.280 parecía "subir" el saldo). Fix en `money_movements.py` (statement endpoint): la **primera pasada recorre TODOS los eventos** calculando el saldo corrido; los eventos anteriores a `date_from` no se listan pero acumulan en `opening_balance`; la fila sintética usa `opening_balance` (no `initial`). Eventos `annulled`/`cancelled` no mueven el balance. Además el response ahora incluye `current_balance` (saldo fresco del tercero) — el frontend (`AccountStatementPage`) lo usa para el KPI "Saldo Contable" en vez del `current_balance` cacheado de la lista de terceros (evita desfase por cache de React Query). Test `test_opening_balance_reflects_movements_before_window`: movimiento de $50K hace 120 días (fuera de ventana) + $10K hoy → `current_balance==60_000`, fila inicial `balance_after==50_000` (apertura, no 0), running balance correcto. NO requiere migración (solo lógica de lectura). **Para entregar estado de cuenta >90 días a un proveedor/cliente: definir `date_from` explícito** — al hacerlo se listan todos los movimientos desde esa fecha y la fila inicial se omite (`not date_from`). **Limitación conocida (mejora pendiente, ver memoria `mejora_cutoff_date_terceros`):** el `initial_balance` es un escalar del momento del corte de migración, sin noción de "antes del corte". Si un usuario registra un movimiento con `date` ANTERIOR a la fecha de migración, el saldo final NO se corrompe (la invariante `initial_balance + Σmovimientos == current_balance` se mantiene), pero la presentación queda inconsistente: en la ventana de 90 días el movimiento pre-corte infla el "Saldo Inicial" sin mostrarse; en vista completa se lista primero pero con el saldo de corte aplicado encima (orden cronológico imposible). No hay validación que lo impida y el corte no se persiste hoy (solo implícito en `created_at`). Fix futuro: persistir `cutoff_date` en terceros migrados (`initial_balance != 0`) y validar/advertir `date < cutoff_date` — NO validar contra `created_at` para terceros normales (es legítimo registrar un movimiento de ayer en un tercero creado hoy).

56. **Fix serialización de arrays en query params (axios → FastAPI)**: `apiClient` (axios 1.6.5, `services/api.ts`) no tenía `paramsSerializer`, así que serializaba arrays con corchetes: `category_ids[]=a&category_ids[]=b`. FastAPI (`Query(list)`) SOLO parsea el formato repeat `category_ids=a&category_ids=b` — con corchetes el parámetro llega como `None` y **el filtro se ignora silenciosamente**. Síntoma reportado: en el Reporte de Gastos (decisión #44), filtrar por categoría padre "Vehiculos" en modo "Sin agrupar (detalle)" no filtraba nada (salían todas las categorías). Afectaba TODOS los filtros multi-select que van por query param (reporte agrupado `expense_category_id[]`/`business_unit_id[]` Y detalle `category_ids[]`/`business_unit_ids[]`). Fix global de una línea: `paramsSerializer: { indexes: null }` en `axios.create` → produce el formato repeat que FastAPI espera, sin afectar params escalares. Los arrays que van en el BODY (POST/PUT, ej. `account_ids` de roles, `applicable_business_unit_ids`) no se ven afectados (paramsSerializer solo aplica a query params). El backend ya filtraba bien (validado contra dev: filtro Vehiculos → solo hijas Kpp-128/CIJ-124/LGU 673/JYN 455/NGY 815, cero fuga) — el bug era 100% de serialización frontend. Sin infra de test frontend (no hay vitest); cobertura backend vía `test_filter_by_parent_category_includes_children` en `test_api_reports.py` (usa TestClient, que serializa en formato repeat correcto). ⚠️ NO quitar el `paramsSerializer` — rompe silenciosamente todos los filtros de array.

57. **Scroll restoration centralizado en `EntityLink`**: El hook `useScrollRestoration` (guarda/restaura el `scrollTop` del `<main id="main-scroll">` en `sessionStorage` con key `scroll:${pathname}${search}`) requiere que la página que navega llame `saveScroll(url)` ANTES de irse y `useScrollRestoration(!isLoading)` al montar. Las páginas de listado (Purchases/Sales/ThirdParties/etc.) ya lo hacían vía `saveScroll` manual en `onRowClick`, pero el estado de cuenta (`AccountStatementPage`) navega a los detalles vía `<EntityLink>` (`<Link>` con `returnTo`), que no guardaba scroll → al hacer "Volver" desde el detalle de una venta/cruce, el estado de cuenta se re-montaba en el top (bug reportado). Fix DRY: `EntityLink` ahora llama `saveScroll(returnTo ?? currentPath)` en su `onClick` — beneficia a las 12 páginas que lo usan sin tocarlas. `AccountStatementPage` gana `useScrollRestoration(!isLoading)`. Como ningún caller pasa `returnTo` explícito a `EntityLink`, siempre guarda `currentPath` (la URL a la que se vuelve). El estado de cuenta scrollea con `main-scroll` (solo `overflow-x-auto` en la tabla, sin scroll-y interno), por eso la restauración aplica. `saveScroll` es inocuo en páginas sin `useScrollRestoration` (deja una entry en sessionStorage que se limpia al cerrar pestaña). Mismo patrón aplicado al drill-down del P&L (requerimiento E, 2026-07-09): `DrillRow` (ProfitAndLossPeriodView) y `CellLink` (ProfitAndLossMonthlyView) llaman `saveScroll(location.pathname + location.search)` en onClick, y ambos views llaman `useScrollRestoration(!isLoading)` — al volver con back del navegador desde /sales o /treasury, el P&L restaura su scroll. Para futuros drill-downs con `<Link>`: siempre acompañar con este par.

58. **UN de sistema "Pasa Mano" (`system_code='double_entry'`) — Rentabilidad por UN sin contaminación DP**: `BusinessUnit.system_code` (nullable, index; migración `120ca61fa631` con backfill que adopta una BU 'Pasa Mano' existente sin materiales o la crea; edge case conocido: si una org tuviera DOS BUs 'Pasa Mano' sin materiales, la adopción marcaría ambas y `get_system_bu` fallaría con MultipleResultsFound — ninguna org actual lo tiene, verificar en migraciones de clientes futuros; seed en `create_organization`). Lookup SIEMPRE por código, nunca por nombre (renombrable). Resuelve 3 fugas del reporte Rentabilidad por UN: (1) `de_profit` prorrateado por-BU eliminado del schema `BusinessUnitProfitability` — la utilidad DP se muda a sección propia; (2) comisiones de ventas DP (llevan `sale_id`) ya no caen en BUs de bodega — el query joinea `Sale.double_entry_id` y las desvía a la sección; (3) fletes/gastos DP se asignan como gasto directo a la UN sistema (única forma soportada). **Guards**: la UN sistema NO acepta materiales (400 en material create/update), NO se puede eliminar (400 en DELETE, sí renombrar), NO participa en `applicable_business_unit_ids` compartidos (422 en los 5 puntos de entrada: `_create_movement` funnel de 23 call sites, ScheduledExpense, FixedAsset, PATCH /classification, ExpenseCategory defaults). Helpers en `services/business_unit.py`: `get_system_bu()`, `validate_not_shared_with_system_bu()`, `validate_not_system_bu()`. **Reporte** (`get_profitability_by_business_unit`): tabla de bodega excluye la UN sistema y sus totales; nueva sección `DoubleEntryProfitability` (ventas/compras/margen DP + comisiones DP + gastos directos asignados + neta); `grand_total_net = totals.net_profit + pasamano_net`. ⚠️ Paridad con P&L: por COMPONENTE (pool de gastos, comisiones split bodega/DP, margen DP == `double_entry_profit`) — el `grand_total_net` NO es igual a `pnl.net_profit` cuando hay líneas no atribuibles a UN: servicios, transformaciones, ajustes de inventario y ajustes de terceros (`pnl.net == grand_total + esas 4 líneas`, conciliado al peso contra prod replica jun-jul 2026: delta $32.26M = 15.25M servicios + 3.19M transformaciones + 0.19M aj. inventario + 13.63M aj. terceros neto). `get_real_cost_by_material` también excluye la UN sistema. ⚠️ `_compute_expense_allocations` (Reporte de Gastos #44) NO se sincroniza: ahí Pasa Mano aparece como grupo natural, es lo deseado. ⚠️ No confundir con el `de_profit` local de `_calculate_profit` (P&L) — intocable. Frontend: `PasaManoCard` en ProfitabilityBUPage, selector compartido excluye UN sistema (`shareableUnits`), selector directo la incluye, MaterialFormDialog la excluye, badge "Sistema" en Config. 24 tests (`TestPasamanoSection`, `TestSystemBUGuards`).

59. **% de gastos generales a Pasa Mano por categoría (`double_entry_general_pct`) + bloque de conciliación P&L**: Columna en `expense_categories` (Numeric(5,2), 0–100, default 0 = comportamiento intacto; migración `f0a7d1e3dd07`). El % vive SOLO en categorías raíz indirectas: subcategorías **heredan en LECTURA** (`_get_dp_pct_by_category`; desviación deliberada del copy-on-create de #36 — editar el padre aplica a todo al instante), 422 si se intenta pct en hija o en directa, y **reparentar zerea el pct** (G1 de QA, junto al copy de `is_direct_expense` en el service update). Mecánica: para cada gasto con alocación GENERAL, `slice = (amt × pct/100).quantize(0.01)` → sección Pasa Mano; el remanente literal `amt − slice` se prorratea entre bodegas (conservación exacta al centavo). Los **3 consumidores** del prorrateo se sincronizan: `get_profitability_by_business_unit` (nueva línea `general_expenses` + detalle `{cat, pct, amount}` en `DoubleEntryProfitability`), `_compute_expense_allocations` (el slice es una alocación `general` a la UN sistema → Reporte de Gastos y drill-down lo muestran solos), y `get_real_cost_by_material` (el slice se DESCARTA del overhead — es costo DP, no de material; su select ganó `expense_category_id`). Sin UN sistema → % se trata como 0 (nada se pierde). Retroactivo por diseño: el % se aplica al leer — cambiarlo re-presenta períodos viejos (aceptado; avisar al cliente). **Bloque de conciliación** (`PnlReconciliation` en el response): las 4 líneas del P&L no atribuibles a UN (servicios, transformaciones neto de merma, ajustes inventario, ajustes terceros neto) + `pnl_net_profit`, derivadas de UNA llamada a `_calculate_profit` — paridad por construcción con el tab P&L. Test de oro anti-drift `test_reconciliation_residual_zero`: `pnl_net == grand_total + Σ(4 líneas)` (tolerancia $1) — si el P&L gana una línea nueva sin actualizar el bloque, revienta. Limitación pre-existente documentada: períodos sin compras no prorratean generales (residual no cierra ahí). Permiso: `treasury.manage_expenses` (PATCH categoría existente, sin permiso nuevo). Frontend: input % en dialog de categorías (oculto en hijas/directas; manda 0 explícito al cambiar a directa), `PasaManoCard` gana línea expandible de generales + sección colapsada "Conciliación con Estado de Resultados" + icono ⚙ a Config, Excel con ambos bloques. 17 tests (`TestDpPctGeneralExpenses`). Smoke vs prod replica: residual $0 exacto con y sin %, grand_total invariante. Plan: docs/planes/plan-pct-generales-pasamano.md.

60. **Utilidad bruta consciente de báscula + fix doble escala de márgenes**: (a) El Reporte de Ventas y el Dashboard calculaban utilidad como `(unit_price − unit_cost) × quantity`, ignorando la cantidad recibida (decisión #18: `total_price = received_quantity × unit_price`) — con diferencias de báscula descuadraba contra el P&L (caso real Costa: $357M vs $342.5M). Fórmula unificada en los 7 sitios: `total_price − unit_cost × quantity` (ingreso con cantidad recibida, COGS con la original — idéntica a `gross_profit_sales` del P&L): `get_sales_report` (totals, by_customer, by_material) y Dashboard (`_period_gross_profit`, top_materials + su order_by, top_customers — cuyo revenue también pasó de `unit_price × qty` a `total_price`). Guardrail: `test_sales_profit_received_quantity_matches_pnl` (reporte dp_filter=exclude == P&L gross, y `profit == revenue − cost` con báscula). (b) `_safe_pct` retorna escala porcentaje (12.6 = 12.6%) — SalesReportPage y MarginAnalysisPage multiplicaban ×100 otra vez (KPI "1264.1%", el "560%" que reportó el cliente) y el Excel del P&L igual en "Margen Neto"; 5 instancias corregidas a `x.toFixed(1)%`. Regla: NUNCA `* 100` sobre valores `*_margin`/`margin_percentage`/`*_pct` del backend — ya vienen multiplicados. (c) KPIs y columnas del Reporte de Ventas renombrados a "Utilidad Bruta"/"Margen Bruto" (pantalla y Excel) — no descuentan comisiones ni gastos.

61. **Fixes de balance histórico (incidente Costa) — el pasado no se reescribe**: cuatro mecanismos reescribían cortes ya consultados (plan `docs/planes/plan-fixes-balance-historico.md`, v2.1 opción B). (a) **FA dados de baja**: `_get_fixed_assets_as_of/_detailed` incluyen `disposed` con `disposed_at >= corte` (helper `_fa_existed_at_cutoff`; `cancelled` sigue excluido per 735c2c3; el detallado sufija "(baja DD/MM/YYYY)" al nombre). (b) **Terceros/cuentas inactivos en cortes**: `_get_tp_balances_as_of` / `_get_account_balances_as_of` y los `tp_objs/acc_objs` de los consumidores históricos incluyen inactivos — existían al corte; desactivar exige saldo 0, así que el fast path (sin as_of) queda intacto. `BalanceDetailedItem.is_inactive` → badge "Inactivo" (web) / sufijo "(inactivo)" (PDF/Excel). (c) **Inventario histórico por fecha de liquidación**: `_get_inventory_as_of` cuenta movimientos de compra/venta por `liquidated_at` de su operación vía EXISTS (ramas huérfanas sin fila padre preservan comportamiento legacy/fixtures; canceladas excluidas SIEMPRE — de paso se arregló el stock fantasma de compras canceladas post-corte); ajustes/transformaciones/transfers siguen por `im.date`. El MCH de `purchase_liquidation` nace con `transaction_date = fecha de liquidación` — ⚠️ usar el PARÁMETRO `(liquidation_date or purchase.date)`: `purchase.liquidated_at` se asigna DESPUÉS del loop de MCH. (d) **`commission_accrual` nace con `date = liquidated_at`** (en ventas `sale.liquidated_at` ya está asignado al llamar `_pay_commissions`; en DPs se pasa `liq_dt` como parámetro — misma trampa de orden). Migración `4d8f2c1e9a7b` realinea MCH + accruals históricos (idempotente, reversible re-derivando de la fecha documento). **Estado de cuenta (#16/#55)**: eventos comerciales (compras/ventas/DPs/comisiones, y sus cancelaciones como par adyacente display-only) posicionados en `liquidated_at`, campo nuevo `document_date` (web sub-línea "doc:", PDF mobile/desktop, Excel columna "Fecha Doc"); la ventana y el saldo de apertura siguen solos al nuevo posicionamiento (filter_dt deriva de txn_date); MMs intactos. **Test de oro**: `test_golden_parity_statement_vs_balance_detailed` (fixture no-trivial: DP + comisión + venta standalone) — saldo corrido del estado de cuenta al corte X == saldo del tercero en balance detallado as-of X. ⚠️ Re-presentación one-time al deploy: los cortes históricos de inventario cambian UNA vez (la semántica vieja contaba operaciones liquidadas tarde en fecha documento) y quedan estables; anticipos/CxC/FA vuelven exactos a los valores pre-incidente (verificado al peso contra réplica Costa). 21 tests (`test_balance_historico_fixes.py`).

62. **Liquidación 2-pasos sin default de fecha (fricción anti back-dating)**: Las 3 Liquidate pages (compra/venta/DP) ya no pre-llenan la fecha con la del documento — el campo arranca vacío y es **obligatorio** (`canSubmit` exige `!!liquidationDate`, botón deshabilitado hasta elegirla, cue visual: label `*`, texto de ayuda rojo→gris, ring rojo). **Por qué**: el default = fecha documento back-dateaba silenciosamente el efecto financiero al liquidar un backlog (registrar en mayo, liquidar en julio → efecto en mayo), reescribiendo cortes históricos pese a #61. El cliente confirmó la regla "la operación cuenta el día que la liquidan", así que el liquidador debe elegirla conscientemente. **Solo frontend**: el backend mantiene el fallback `liquidation_date or document.date` (#42) — necesario para `auto_liquidate` (1-paso, CreatePages: crear+liquidar mismo momento, sin back-dating) y la migración (siembra vía ajustes, no liquida). `min={docDateStr}`/`max={todayStr}` (documento..hoy) preservados. ⚠️ NO re-agregar un default en las Liquidate pages — deshace la fricción. Sin tests (frontend sin infra de test; verificado por lectura + `tsc`).

63. **Cancelar venta/compra: elección sobre el cobro/pago inmediato enlazado**: Al cancelar una venta/compra **liquidada** con cobro/pago inmediato enlazado (`collection_from_client`/`payment_to_supplier` con `sale_id`/`purchase_id`), el operador elige **sin default** entre anular también el movimiento de caja o dejarlo como anticipo/prepago. **Por qué**: es la decisión #16 "Liquidación ≠ Pago" — el `cancel` revierte lo comercial pero el pago es una operación separada; antes quedaba vivo **en silencio** (caja + / tercero como anticipo), defendible si el dinero se movió, engañoso si fue error de captura. Se descartó "anular siempre" (rompe el anticipo legítimo del cliente que sí pagó) y el 422 duro en backend (rompería `auto_liquidate` sin ganar seguridad real — el frontend es el único cliente humano del 2-pasos). **Mecánica backend** (simétrico): flag `annul_linked_payments: bool=False` en `cancel()` (servicio) + endpoints `PATCH /{id}/cancel` (Query). Al anular, **revierte exactamente el efecto del movimiento** — venta (cobro fue caja+/cliente−) → caja−/cliente+; compra (pago fue caja−/proveedor+) → caja+/proveedor− (signos **opuestos**); marca `annulled` con auditoría; **solo `confirmed`** → no re-anula uno ya anulado a mano, no-op seguro sin enlace. Mismo patrón que la anulación de `commission_accrual` que el `cancel` ya hacía (sin permiso extra). Detección: `get_linked_payment_total()` expuesto como `linked_payment_total` (Optional[float]=None) **solo en los 2 GET de detalle** → sin N+1 en el listado. **Frontend**: componente compartido `LinkedPaymentChoice` (panel ámbar + 2 radios sin selección inicial, parametrizado venta/compra) usado por DetailPages **y** listados; en el listado `ActionsCell` consulta el detalle **on-demand** (`useSale`/`usePurchase` con `enabled: cancelOpen && isLiquidated`, sin fetch por fila) para el monto. Botón deshabilitado hasta elegir (`annulChoice===null`). `ConfirmDialog` ganó `onClick stopPropagation` en su contenido — el diálogo se renderiza en un **portal** pero los clicks burbujean por el árbol de React hasta el `onRowClick` de la fila; sin esto, tocar un radio navegaba al detalle. **Limitación conocida**: cobro/pago **posterior manual** (MoneyMovement suelto contra el tercero, `sale_id`/`purchase_id` NULL) NO es rastreable → sigue manual; el diálogo solo cubre el **inmediato enlazado** (Costa: ~91 inmediatos vs ~395 posteriores). 10 tests (`TestCancelSaleWithLinkedCollection` 6 + `TestCancelPurchaseWithLinkedPayment` 4): assert clave = **round-trip al origen** (annul=True devuelve caja y tercero al estado pre-operación; el signo invertido lo atraparía).

64. **Modelo L — COGS de venta finalizado al liquidar (fix estructural costo promedio, PR-1 de 4)**: Regla del modelo (confirmada por Daniel 2026-07-09): **"nada existe financieramente hasta que se liquida"**. El `unit_cost` capturado al registrar la venta pasa a ser **provisional** (utilidad estimada); `sale.liquidate()` lo finaliza al `current_average_cost` vigente (fallback `_get_last_known_cost` si 0), actualizando `SaleLine.unit_cost` + el `InventoryMovement` espejo (matching por firma `(material_id, -qty)` con deque — patrón QW-B; la venta es mono-bodega). Extraer al promedio NO cambia el promedio → sin MCH. Consecuencia aceptada: el COGS **depende del orden de liquidación** (acotado por la diferencia de precios de compras solapadas); el invariante es la **conservación de valor** (COGS + inventario == valor entrado — test de oro en `test_avg_cost_model_l.py` con AMBOS órdenes). La "Faceta 2" del bug histórico (compras promediando contra pool con ventas registradas dentro, caso Clausen) queda **re-etiquetada como comportamiento correcto** del modelo. Se descartó el Modelo P (COGS al registrar): exigiría restar `liquidated` al registrar → pool negativo diario en el flujo báscula → promedios absurdos (veto 2026-06-16). DP intocable (COGS = precio de cruce, servicio propio; `sale.liquidate` rechaza DPs). Rounding conocido pre-existente: `unit_cost` Numeric(15,2) vs avg (15,4) → ≤$0.005/kg. ⚠️ **PR-1 NO se deploya sin PR-2** (helper de oversell): Modelo L sin manejo de pool negativo pisa la rama reset de `_apply_cost_at_liquidation` con más frecuencia. Plan completo (fases 2-4: conservación de valor, warnings, remediación histórica): `docs/planes/plan-fix-estructural-costo-promedio.md`. **Fase 4 remediación DESCARTADA por decisión de negocio (Daniel 2026-07-10)**: las reglas del Modelo L aplican desde el deploy hacia adelante; el COGS histórico queda con el método viejo (coherente con #61 "el pasado no se reescribe"; magnitud <1%, cubierta por `docs/justificacion-cliente-diferencias-inventario.md`). También descartada la remediación QW-B #1074/#1295 por coherencia.

65. **Conservación de valor en oversell (fix estructural PR-2 de 4)**: Helper puro `incorporate_into_pool(liquidated, avg, qty, unit_cost) → (new_avg, cost_adjustment)` en `services/inventory_costing.py` reemplaza las ramas reset que **borraban valor** cuando el pool estaba negativo. Ecuación del asiento (exacta, 3 ramas): `pool_after == pool_before + entrada + adjustment`; hueco no cubierto → avg queda en el previo. Consumidores: (a) `purchase._apply_cost_at_liquidation` retorna el adjustment y el loop per-línea lo persiste en `purchase_lines.cost_adjustment` — el helper recibe el costo AJUSTADO por comisión (G1) y ve el pool corriente per-línea (G2); (b) `sale.cancel` de liquidada hace reingreso **PONDERADO** con `line.unit_cost` (el COGS devuelto vuelve como VALOR — mata la re-valuación fantasma al promedio vigente) + MCH **`sale_cancellation`** (5º source_type, solo si el avg cambió; su rol bloqueante sobre `check_can_revert` quedó superseded por #66 — el guard fue retirado) + `sales.cancellation_cost_adjustment` si rellenó hueco. **P&L**: línea nueva "Ajuste Costo por Sobreventa" (`oversell_cost_adjustment`) = Σ compras liquidadas por `liquidated_at` (**G3**: puede aterrizar en un mes distinto al de la venta que sobrevendió — comunicar al cliente) + Σ cancelaciones por `cancelled_at` (se cuenta aunque la venta esté cancelled — deliberado, es el efecto real que dejó en inventario); suma a `total_gross_profit`; **conciliación #59 pasa a 5 líneas** (org-level, no atribuible a UN — G4). Migración `8548fcde95ee` (2 columnas `server_default=0` → P&L histórico NO cambia al deploy). Frontend: fila condicional ≠0 en P&L periodo/mensual + Excel + 5ª línea conciliación. Guardrail: stress walk determinista (`TestInventoryStressWalk`, 60 ops, 5 invariantes globales tras cada una — stock=transit+liquidated, stock=Σmovimientos, avg≥0, avg=último MCH, conservación). Paridad prod-vs-dev verificada: balance actual **delta $0 en 44 campos** (réplica fresca 2026-07-10). ⚠️ **Deploy: PR-1 (#64) + PR-2 JUNTOS, nunca PR-1 solo (G5)**. 11 tests nuevos. **Fase 3 (PR-3)**: cancelar compra liquidada cuyo material ya fue vendido **avisa sin bloquear** (#17) — `purchase.cancel()` retorna `(purchase, warnings)` (patrón create/update), warning por material con estado final ("queda negativo tras la cancelación"), toast ámbar en frontend. 2 tests. **Fase 2c (PR-4)**: ajustes `increase` y líneas destino de transformación adoptan el helper — `InventoryAdjustment.cost_adjustment` + `MaterialTransformationLine.cost_adjustment` (migración `7c2f9a41d8e3`, `server_default=0`), 2 fuentes nuevas en la MISMA línea P&L (status `confirmed`, fechadas por fecha de ajuste/transformación; seeds de migración quedan fuera solos: entran a pool 0 → adjustment 0; sin exposición en response schemas, igual que compras). Método `average_cost` de transformación es auto-neutral (el destino entra a su propio avg → adjustment 0); aplica con `proportional_weight`/`manual`. MCH de increase se registra SIEMPRE (el annul revierte vía ese registro; anulado sale del P&L por status). `decrease`/`zero_out` ya conservaban; `recount` mantiene su inconsistencia pre-existente (pondera con `old_total`, fuera de alcance). ⚠️ Gap pre-existente ahora visible: `check_can_revert` no ve extracciones MCH-silenciosas (ventas liquidadas/decreases no crean MCH) → revert de increase/compra es inexacto si hubo extracción entre medias; el stress walk invalida candidatos a annul tras cada op MCH-silenciosa; memoria `bug_check_can_revert_falsos_permisos`, candidata Fase 5. 5 tests + walk extendido con acciones de ajustes.

66. **Remoción ponderada en reversiones — retiro de `check_can_revert` y del rewind (Fase 5)**: Cancelar compra liquidada / anular ajuste / anular transformación ya NO rebobinan el costo vía MCH — el rewind era exacto solo si nada extrajo valor entre la operación y su reversión, y bajo Modelo L las ventas liquidadas y los decreases son MCH-silenciosos → el guard daba falsos permisos y fugaba valor (réplica: 13/18 compras canceladas-liquidadas tenían ventas entre medias; 79% de las liquidadas estaban bloqueadas — bloqueaba mucho, protegía poco). Ahora: helper espejo `remove_from_pool(liquidated, avg, qty, unit_cost) → (new_avg, adjustment)` en `inventory_costing.py` — ecuación exacta `pool_after == pool_before − q×u + adj` en 3 ramas (remoción limpia = inverso de incorporate; valor insuficiente o cruce a hueco: avg queda y `adj = q×(u−A)`). **Nunca bloquea**: `check_can_revert` y `revert_cost_change` eliminados (los errores "Cancele primero: …" desaparecen). **MCH append-only puro**: las reversiones escriben su registro SIEMPRE (incondicional — sin él la cadena as-of pierde el avg vivo tras cancelar; a diferencia de `sale_cancellation` que sigue condicional porque su original nunca escribió MCH); 3 source_types nuevos (`purchase_cancellation`, `adjustment_annulment`, `transformation_annulment` → 8 total). **H1**: la remoción saca la contribución REAL — costo ajustado por comisión leído del `InventoryMovement` vía deque-por-firma (single source of truth) + `cost_adjustment/qty` del fill; annul de decrease/zero_out reingresa PONDERADO a `adjustment.unit_cost` (avg de la salida, no el vigente); fuente de transformación anulada reingresa a `source_unit_cost`. Diferencias en columnas nuevas `purchases.cancellation_cost_adjustment` / `inventory_adjustments.annul_cost_adjustment` / `material_transformations.annul_cost_adjustment` (migración `4530b4e47938`, `server_default=0` → P&L histórico intacto) → misma línea P&L por `cancelled_at`/`annulled_at` (7 fuentes; G3 aplica; label frontend "Ajuste Costo por Sobreventa y Reversiones"). **H2**: `_get_inventory_as_of` filtra MCH de ops cancelled/annulled con dualidad por nivel — camino principal los excluye (reversiones pasan), Fallback 1 los INCLUYE (su `previous_cost` = avg pre-op es evidencia válida) y excluye los 3 tipos de reversión Fase 5 (`sale_cancellation` sí pasa). No-op para datos existentes → el deploy no re-presenta cortes. Consecuencia visible: cancelar con actividad posterior deja el avg al vigente, NO "al número de antes" (el P&L se redistribuye entre fechas, el total conserva — comunicable igual que G3). Supersede #9 (rewind+bloqueo), #40 y el rol bloqueante de #65. Guardrail estrella: stress walk SIN restricciones (la regla de invalidación de PR-4 eliminada). 12 tests nuevos + 11 tests existentes con semántica actualizada (tabla en `docs/planes/reporte-qa-pr5-remocion-ponderada.md`). Plan: `docs/planes/plan-fase5-remocion-ponderada.md`.

67. **Revalorización de activos fijos (alza/baja con contrapartida cuenta o tercero)**: `POST /fixed-assets/{id}/revalue` (permiso `treasury.manage_fixed_assets`) ajusta `current_value` **sin tocar P&L** — mejora capitalizable / recuperación de valor, NO revaluación NIIF a patrimonio. Mecánica espejo XOR de #21: alza = `asset_revaluation_payment` (cuenta−) o `asset_revaluation_credit` (tercero− → pasivo); baja = `asset_devaluation_collection` (cuenta+) o `asset_devaluation_receivable` (tercero+ → CxC). Catálogo MM pasa a **25 tipos**; los 4 nuevos entran a `ASSET_MOVEMENT_TYPES` (anular directo en Tesorería → 422; el frontend oculta el botón y muestra nota guía hacia el activo), NO entran a `EXPENSE_MOVEMENT_TYPES` (conciliación #59/#65 intacta), y la **terna de signos está alineada en 6 sitios**: efecto vivo en `revalue()`, `ACCOUNT/THIRD_PARTY_BALANCE_DIRECTION` (reports.py), los 2 mapas duplicados del endpoint de statements (money_movements.py), `INFLOW/OUTFLOW_TYPES` (cash flow opening + dashboard MTD). Cash flow: `asset_payments` absorbe el pago de reval; inflow nuevo `asset_devaluation_collections`. Tabla `asset_revaluations` (migración `8d1a27b7bb31`): evento auditado con snapshots `value/monthly_before/after`, `money_movement_id` NOT NULL (RESTRICT), `is_active` + auditoría de anulación; `period` es **display-only — NUNCA usarlo para math as-of**. Recalculo de cuota desde el mes siguiente: `remaining = ceil((value−salvage)/monthly)`, `remaining_after = remaining + months_extended` (solo alza), `monthly_after = (value_after−salvage)/remaining_after`; alza sobre `fully_depreciated` exige `months_extended ≥ 1`; baja tope `value − salvage` (piso residual); estados derivados de `value vs salvage`, no de flags. Fecha del evento SIEMPRE hoy (anti back-dating #62, sin input de fecha). **Anulación con guard LIFO ampliado**: bloquea si existe CUALQUIER evento activo posterior (depreciación O reval, por `applied_at`) → restauración por snapshots exacta por construcción (contraste deliberado con #66: aquí el ledger de eventos es completo → bloqueo exacto, sin falsos permisos); `cancel()` del activo revierte revals activas primero. **Balance histórico (H1): reconstrucción pura con ancla DIARIA** — `_fa_value_at_cutoff = current_value + Σ dep.amount(period > corte_mensual) − Σ reval_firmada(MM.date >= corte_dt)` vía `_fa_reval_future_delta` (JOIN al MoneyMovement): depreciaciones anclan mensual (#41), revalorizaciones por fecha del MM — el MISMO boundary diario que los saldos as-of de cuentas/terceros → el balance cuadra en TODO corte, incluso intra-mes. Supera el diseño v1 (merge por `period`) que descuadraba el corte de ayer — bug encontrado en pruebas de usuario. As-of deriva `acc_dep = purchase + reval_delta(≤corte) − valor_al_corte`; `revalued_amount` expuesto en vivo (query agregada, sin N+1) y as-of. UI: `RevalueAssetModal` (preview espejo de la fórmula, warning de depreciaciones pendientes — la cuota nueva rige para lo que se aplique después; copy que explica dónde aterriza la contrapartida: alza a crédito de un tercero con saldo a favor primero CONSUME ese saldo, #31), sección Revalorizaciones en el detail del activo (anular desde ahí), labels índigo en Tesorería. 26 tests (`test_asset_revaluation.py`) incl. golden corte-de-ayer + estabilidad del balance general y round-trip de anulación.

### Inventory Module — UX Details

- **StockPage**: Manual `<Table>` (no DataTable) con expandable rows por bodega. Filters: Category, Warehouse, Search. Acciones: Trasladar, Ver Movimientos, Ajustar Stock (navegan con `?material_id=`).
- **MovementHistoryPage**: Filters Material + Warehouse al backend. Con `material_id`: muestra `balance_after` y `avg_cost_after` (running balance iterativo).
- **TransitPage**: KpiCards + bottleneck alerts + 2 DataTables (compras/ventas pendientes).
- **AdjustmentCreatePage**: Lee `material_id` de URL search params para pre-seleccionar material.
